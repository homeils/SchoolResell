<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    tools:context=".SellActivity">

    <LinearLayout
        android:id="@+id/sell_pic_container"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:background="@drawable/sell_pic_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="Orientation" />

    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/sell_pic_add"
        app:layout_constraintTop_toTopOf="@id/sell_pic_container"
        app:layout_constraintBottom_toTopOf="@id/sell_pic_hint"
        app:layout_constraintLeft_toLeftOf="@id/sell_pic_container"
        app:layout_constraintRight_toRightOf="@id/sell_pic_container"/>

    <TextView
        android:id="@+id/sell_pic_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="上传图片"
        app:layout_constraintBottom_toBottomOf="@id/sell_pic_container"
        app:layout_constraintLeft_toLeftOf="@id/sell_pic_container"
        app:layout_constraintRight_toRightOf="@id/sell_pic_container" />

</androidx.constraintlayout.widget.ConstraintLayout>
